import Button from './index.vue'
import type { ButtonEmits, ButtonProps, ButtonType, ButtonSize } from './types'

export { Button, ButtonProps, ButtonType, ButtonSize, ButtonEmits }
export default Button
