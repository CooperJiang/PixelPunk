import File from './index.vue'
import type { FileBackgroundPattern, FileEmits, FileFitMode, FileInfo, FileProps } from './types'

export default File
export { File }
export type { FileProps, FileEmits, FileInfo, FileFitMode, FileBackgroundPattern }
