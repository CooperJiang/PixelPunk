import MultiSelector from './index.vue'
import type { MultiSelectorEmits, MultiSelectorProps, SelectorOption } from './types'

/* 默认导出组件 */
export default MultiSelector

/* 具名导出组件 */
export { MultiSelector }

/* 导出所有类型 */
export type { MultiSelectorProps, MultiSelectorEmits, SelectorOption }
