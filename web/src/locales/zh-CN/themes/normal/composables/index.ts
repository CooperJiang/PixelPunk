import { performance, chunkedUpload } from './performance'
import { useShareData } from './useShareData'
import { useUploadConfig } from './useUploadConfig'
import { useGlobalUpload } from './useGlobalUpload'

export const composables = {
  performance,
  chunkedUpload,
  useShareData,
  useUploadConfig,
  useGlobalUpload,
}
